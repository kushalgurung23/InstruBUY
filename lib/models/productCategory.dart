import 'package:flutter/material.dart';

class ProductCategory {
  final String category_id, category_name, category_image;

  ProductCategory(
      {@required this.category_id,
      @required this.category_name,
      @required this.category_image});
}
